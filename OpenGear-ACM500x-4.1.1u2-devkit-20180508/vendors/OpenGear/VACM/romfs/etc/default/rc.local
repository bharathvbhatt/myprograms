#!/bin/sh
#
# This script is executed after all the other init scripts, and is where you
# should put any customized startup items you require.
#

if [[ ! -f /etc/config/scripts/.configured ]]; then
	/etc/config/scripts/demo-config
	touch /etc/config/scripts/.configured
fi
