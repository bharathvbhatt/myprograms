*** Welcome to your virtual Opengear demo! ***

This system contains an example configuration, you may login via HTTP, HTTPS
or SSH using any of these preconfigured accounts:

  root / default (superuser)
  administrator / default (administrative user)
  operator / default (basic user)

Note this is a virtual system, and physical connections such as serial consoles
and other managed devices are simulated. For a live demo, please contact us at:
http://opengear.com/contact-opengear
