boot=/dev/sda
root=/dev/sda1
map=/boot/map
install=text
timeout=50
delay=30
default=normal
lba32
compact
message=/etc/lilo.message
password="VaCm"

image=/boot/vmlinuz
	append="loglevel=3 console=ttyS0"
	label=normal
	read-only
	restricted

image=/boot/vmlinuz
	label="erase"
	append="erase-config"
	read-only
	mandatory

